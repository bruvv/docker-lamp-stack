# Please Note:
# In PHP Versions <= 7.4 MySQL8 is not supported due to lacking pdo support

# To determine the name of your containers
COMPOSE_PROJECT_NAME=lamp

# Possible values: php54, php56, php71, php72, php73, php74, php80, php81, php-latest
PHPVERSION=php81
DOCUMENT_ROOT=./www
VHOSTS_DIR=./config/vhosts
PHP_INI=./config/php/php.ini
NGINX_CONF=./config/nginx/nginx.conf

# PHPMyAdmin
UPLOAD_LIMIT=512M
MEMORY_LIMIT=512M

# Possible values: mysql57, mysql8, mariadb103, mariadb104, mariadb105, mariadb106, mariadb-latest
#
# For Apple Silicon User: 
# Please select Mariadb as Database. Oracle doesn't buid their SQL Containers for the arm Architecure

DATABASE=mariadb106
MYSQL_INITDB_DIR=./config/initdb
MYSQL_DATA_DIR=./data/mysql
MYSQL_LOG_DIR=./logs/mysql

# MySQL root user password
MYSQL_ROOT_PASSWORD=tiger

# Database settings: Username, password and database name
#
# If you need to give the docker user access to more databases than the "docker" db 
# you can grant the privileges with phpmyadmin to the user.
MYSQL_USER=docker
MYSQL_PASSWORD=docker
MYSQL_DATABASE=docker

# If you want specific port please edit the docker compose and set port below
HOST_MACHINE_UNSECURE_HOST_PORT=80

# If you want specific port please edit the docker compose and set port below
HOST_MACHINE_SECURE_HOST_PORT=443

# If you want specific port please edit the docker compose and set port below
HOST_MACHINE_MYSQL_PORT=3306

# If you want specific port please edit the docker compose and set port below
HOST_MACHINE_PMA_PORT=8080

# If you want specific port please edit the docker compose and set port below
HOST_MACHINE_REDIS_PORT=6379
